# 🌐 Variables de Entorno para Sistema de Gestión de Panadería
# Copiar este archivo a .env para desarrollo local
# Para Railway: Configurar estas variables en el dashboard

# === BASE DE DATOS ===
# Desarrollo local (SQLite)
DATABASE_URL="file:./dev.db"
# Producción Railway (PostgreSQL - Railway lo genera automáticamente)
# DATABASE_URL="postgresql://usuario:contraseña@host:puerto/nombre_bd"

# === SEGURIDAD (CRÍTICO - GENERAR SECRETS ÚNICOS) ===
JWT_SECRET="genera-un-secreto-de-al-menos-32-caracteres-aleatorios"
NEXTAUTH_SECRET="otro-secreto-diferente-de-al-menos-32-caracteres"

# === ENTORNO ===
NODE_ENV="production"

# === SERVIDOR ===
HOSTNAME="0.0.0.0"
PORT=3000
NEXT_TELEMETRY_DISABLED=1

# === CREDENCIALES INICIALES DEL SISTEMA ===
ADMIN_EMAIL="admin@tuempresa.com"
ADMIN_PASSWORD="TuContraseñaSuperSegura123!"
COMPANY_NAME="Tu Panadería"

# === OPCIONAL: EMPLEADO DEMO ===
EMPLOYEE_EMAIL="empleado@tuempresa.com"
EMPLOYEE_PASSWORD="EmpleadoDemo123!"

# === CONFIGURACIÓN REGIONAL ===
DEFAULT_TIMEZONE="America/Mexico_City"
DEFAULT_CURRENCY="MXN"
DEFAULT_LANGUAGE="es-MX"

# Production Settings (uncomment for deployment)
# DATABASE_URL="file:/app/data/production.db"
# NODE_ENV="production"
# NEXT_TELEMETRY_DISABLED=1
# HOSTNAME="0.0.0.0"
# PORT=3000